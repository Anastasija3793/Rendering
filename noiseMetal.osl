shader noiseMetal (
	color Cin=1,
output	color Cout=1
//output float resulF=0
)
{
    point PP=transform("referenceFrame",P);
    //Cout=(color)noise("uperlin",PP*10);
    //Cout=(float)noise("uperlin",PP[0]*100)+0.5;



    float freq=1;
    Cout=0.0;
    int i;
    for(i=0;i<8;i++)
	{
	    Cout+=abs((float)noise("uperlin",PP[0]*freq)/freq);
	    //Cout=(float)noise("uperlin",PP[0]*100);
	    freq*=2.1;
	}

}

