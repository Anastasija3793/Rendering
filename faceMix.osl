shader faceMix(
	color c1=0,
	color c2=0,
output  color Cout=0
	)
{
normal NN=transform("referenceFrame",N);
//if(NN[1]<0)
//	if(c2[0] >= 0.7)
//	Cout = mix(c1, c2, noise("perlin", P)*0.2);
//else
//	Cout=c2;








//if(NN[1]<0)
//	Cout=c1;
//else
// Cout=c2;



if(c2[0] < 0.7)
//< 0.2
	Cout = c2;
else
	Cout = mix(c1, c2, noise("perlin", P)*0.2);
}


//creat ONE shader and get rid of the others!

//try smth like out=noise*metalimage
